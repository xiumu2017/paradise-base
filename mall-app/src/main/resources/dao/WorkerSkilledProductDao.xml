<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.portal.dao.WorkerSkilledProductDao">
    <resultMap id="skilledProductInfo" type="com.macro.mall.portal.domain.WorkerSkilledProductInfo"
               extends="com.macro.mall.mapper.YxxWorkerSkilledProductMapper.BaseResultMap">
        <result column="productName" property="productName" jdbcType="VARCHAR"/>
    </resultMap>
    <select id="list" resultMap="skilledProductInfo">
        select ywsp.id, ywsp.worker_id, ywsp.product_id, p.name productName
        from yxx_worker_skilled_product ywsp
                     left join pms_product p on ywsp.product_id = p.id
        where ywsp.worker_id = #{workerId,jdbcType=BIGINT}
        order by ywsp.id
    </select>
</mapper>